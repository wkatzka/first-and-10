# Frontend Environment Variables
# Two environments only: "testing environment" and "live environment". See ENVIRONMENTS.md.

# ============== TESTING ENVIRONMENT (local: localhost:3000 → backend localhost:4000) ==============
NEXT_PUBLIC_API_URL=http://localhost:4000
# Optional: force testing even if URL is not localhost
# NEXT_PUBLIC_APP_ENV=development

# ============== WALLET / CRYPTO (local only) ==============
# Pack contract on Base Sepolia
# NEXT_PUBLIC_PACK_CONTRACT_ADDRESS=0x7Dc1a1e4240248B77BcE32DbFb39aB1b2b8007B3

# Web3Auth Client ID (for Email/Social wallet login)
# Get yours at https://dashboard.web3auth.io
# NEXT_PUBLIC_WEB3AUTH_CLIENT_ID=your_client_id_here

# Coinbase Pay App ID (for buying ETH - mainnet only, optional)
# Get yours at https://pay.coinbase.com/onramp
# NEXT_PUBLIC_COINBASE_APP_ID=your_app_id_here

# ============== LIVE ENVIRONMENT (deployed frontend → deployed backend) ==============
# Use when building/deploying the live app. Do not use localhost.
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
# NEXT_PUBLIC_APP_ENV=production

# Enable crypto shop on live (set to "true" to show Shop page in production)
# NEXT_PUBLIC_CRYPTO_SHOP_ENABLED=true
