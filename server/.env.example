# Server Environment Variables
# Two environments only: "testing environment" and "live environment". See ENVIRONMENTS.md.

# ============== TESTING ENVIRONMENT ==============
# Frontend origin for CORS (Next.js on port 3000). Optional; localhost:3000 is already allowed.
# FRONTEND_URL=http://localhost:3000

DATABASE_URL=postgresql://user:password@localhost:5432/firstand10

# ============== LIVE ENVIRONMENT ==============
# Set FRONTEND_URL to your deployed frontend so CORS allows it.
# FRONTEND_URL=https://your-app.vercel.app
# DATABASE_URL=... (your production Postgres)

# ============== Optional ==============
# NFT_CONTRACT_ADDRESS=0x7Dc1a1e4240248B77BcE32DbFb39aB1b2b8007B3
# CHAIN_ID=84532
# BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# PRIVATE_KEY=0x... (test wallet only; never commit real keys)
